# =============================================================================
# MOMENT 97 — fast regression smoke profile
# =============================================================================
model_type: "moment"
model_path: "AutonLab/MOMENT-1-small"
training_mode: "fine_tune"
context_length: 384
forecast_length: 96
use_wrapper_normalization: false
covariate_cols: []

learning_rate: 1.0e-4
batch_size: 16
num_epochs: 1

window_stride: 96
max_train_windows: 2048

target_col: "bg_mM"
interval_mins: 5
