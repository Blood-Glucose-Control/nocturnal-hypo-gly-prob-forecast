# MOMENT Sweep 13 - BG + carb_availability only (data ablation)
model_path: "AutonLab/MOMENT-1-small"
training_mode: "fine_tune"
context_length: 512
forecast_length: 96
use_wrapper_normalization: false
covariate_cols: ["carb_availability"]

learning_rate: 1.0e-4
batch_size: 16
num_epochs: 5

window_stride: 96
max_train_windows: 4096

target_col: "bg_mM"
interval_mins: 5
