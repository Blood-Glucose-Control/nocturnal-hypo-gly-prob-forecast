# =============================================================================
# TimesFM 97 — fast regression smoke profile
# =============================================================================
model_type: "timesfm"
training_mode: "fine_tune"
context_length: 256
forecast_length: 96

loss_fn: "pinball"
learning_rate: 1.0e-5
num_epochs: 1
batch_size: 256
gradient_accumulation_steps: 2
weight_decay: 0.01
torch_dtype: "bfloat16"

target_col: "bg_mM"
interval_mins: 5
imputation_threshold_mins: 45
freq_type: 0
