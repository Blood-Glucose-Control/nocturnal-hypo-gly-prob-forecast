# P1-37 End-of-Day Handoff — 2026-08-16

## Session scope

Continued Phase F/P1 scripts cleanup after merge of PR #440, with focus on:

1. reducing duplicated model sweep shell orchestration,
2. removing remaining legacy compatibility entrypoints, and
3. migrating context-ablation scripts into canonical sub-experiment locations.

## Completed today

### A) Deep model sweep dedup to profile-driven wrappers

- Added train/eval sweep profile specs:
  - `configs/experiments/nocturnal_forecast/deepar_forecasting_{train,eval}_sweep.yaml`
  - `configs/experiments/nocturnal_forecast/patchtst_forecasting_{train,eval}_sweep.yaml`
  - `configs/experiments/nocturnal_forecast/tft_forecasting_{train,eval}_sweep.yaml`
- Replaced heavy DeepAR/PatchTST/TFT model sweep shell scripts with thin wrappers:
  - `scripts/training/sweeps/models/{deepar,patchtst,tft}_sweep_train.sh`
  - `scripts/evaluation/sweeps/models/{deepar,patchtst,tft}_sweep_eval.sh`
- Updated overnight deep chain to parse YAML sweep specs instead of shell-embedded arrays:
  - `scripts/orchestration/sweeps/run_overnight_deep_sweeps.sh`
- Removed remaining orchestration compatibility wrappers from `scripts/experiments/`:
  - `chronos2_sweep.sh`
  - `run_ctx_ablation_sweeps.sh`
  - `run_overnight_deep_sweeps.sh`

### B) Ctx-ablation migration to canonical sub-experiment paths

- Migrated ctx-ablation train scripts from `scripts/experiments/` to:
  - `scripts/training/sweeps/nocturnal_forecast/context_ablation/`
- Migrated ctx-ablation eval scripts from `scripts/experiments/` to:
  - `scripts/evaluation/sweeps/nocturnal_forecast/context_ablation/`
- Rewired chain orchestrator to those canonical paths:
  - `scripts/orchestration/sweeps/run_ctx_ablation_sweeps.sh`
- Updated moved scripts’ repo-root resolution and usage/help comments.

## Validation performed

- `bash -n` pass on all modified/new shell scripts for both passes.
- Dry-run checks completed for new profile-driven wrappers and overnight chain:
  - `deepar_sweep_train.sh` (dry-run)
  - `patchtst_sweep_train.sh` (dry-run)
  - `tft_sweep_train.sh` (dry-run)
  - `patchtst_sweep_eval.sh` (dry-run)
  - `tft_sweep_eval.sh` (dry-run)
  - `run_overnight_deep_sweeps.sh` (dry-run)
- Fixed one surfaced bug in chain summary count (`wc -l` instead of `grep -c ... || echo 0`).

## Git status summary

- Branch: `p1-profile-driven-sweep-launcher-dedup`
- Committed and pushed:
  - `b584447` — deep-model profile-spec dedup + orchestration wrapper cleanup
- Additional ctx-ablation migration edits are currently in working tree (not yet PR’d at this checkpoint).

## Recommended first steps tomorrow

1. Re-run quick validation:
   - `bash -n` on modified ctx-ablation scripts and `run_ctx_ablation_sweeps.sh`.
2. Stage/commit ctx-ablation migration + docs/tracking updates.
3. Push branch update and open/refresh PR for review.
4. Follow-on cleanup target:
   - converge remaining model sweep scripts (Moirai/Moment/TiDE/TimesFM/Toto/Statistical/Naive/Sundial + Chronos special evals) toward profile-driven wrappers where feasible.
