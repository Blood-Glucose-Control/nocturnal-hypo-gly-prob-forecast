# P1-37 Handoff — 2026-08-14 (End of Day)

## Morning update — 2026-08-14 (Phase C kickoff)

- PR #433 merged to `main`.
- Local `main` fast-forwarded to `713f577`.
- Local cleanup completed:
  - deleted `p1-scripts-phase-b`
  - deleted `p1-scripts-phase-b2`
- New branch created for next scope:
  - `p1-scripts-phase-c-launcher-rewire`
- Plan refreshed in:
  - `agent_notes/P1-37_SCRIPTS_REORG_AND_PRUNE_EXECUTION_PLAN.md`

## 1) Where we are right now

- Active branch: `p1-scripts-phase-b2`
- Open PR: #433
  https://github.com/Blood-Glucose-Control/nocturnal-hypo-gly-prob-forecast/pull/433
- `main` currently includes:
  - Phase A cleanup merge (#431)
  - Phase B prune/path normalization merge (#432)
- PR #433 stacks additional Phase B hardening and test-contract fixes.

## 2) What landed on `p1-scripts-phase-b2` (not yet on `main`)

1. Shared AutoGluon env convention (`.venvs/autogluon`) across scripts/tests/docs.
2. `scripts/setup_model_env.sh` alias/dependency-group resolution hardening.
3. Removed redundant `scripts/setup_moirai_env.py`.
4. `supports_zero_shot` normalized as a property across model surfaces.
5. Added guard test:
   - `tests/models/test_supports_zero_shot_contract.py`
6. Chronos2 triage fixes:
   - zero-shot quantile `None` handling
   - stale AutoGluon file-handler cleanup before predictor creation
   - test alignment to current model API
7. Registered pytest `slow` marker.
8. Applied same stale-handler guard to TiDE training path.

## 3) Validation status (latest)

- AutoGluon-family subset pass:
  - `tests/models/test_autogluon_base.py`
  - `tests/models/test_chronos2.py`
  - `tests/models/test_deepar.py`
  - `tests/models/test_patchtst.py`
  - `tests/models/test_tft.py`
  - `tests/models/test_naive_baseline.py`
  - `tests/models/test_statistical.py`
- Result: **79 passed** (warnings only; no failures).

## 4) First actions tomorrow

1. Review and merge PR #433.
2. Fast-forward local `main`.
3. Start next branch for Phase C (launcher hardening), suggested:
   - `p1-scripts-phase-c-launcher-rewire`

## 5) Immediate next scope (Phase C)

Goal: replace deprecated/fail-fast launcher paths with one maintained entrypoint.

Suggested sequence:

1. Select canonical Python training entrypoint (do not split yet).
2. Rewire:
   - `scripts/training/slurm/single_gpu.sh`
   - `scripts/training/slurm/multi_gpu.sh`
   - `scripts/training/slurm/adaptive_resources.sh`
3. Keep CLI flags/inputs stable where possible.
4. Update:
   - `scripts/training/slurm/README.md`
   - `docs/user-guide/example-scripts-guide.md`
5. Validate with:
   - `bash -n` for touched shell scripts
   - targeted no-op/help invocation for rewired command path
   - focused experiment summary tests

## 6) Notes / caveats

- `agent_notes/`, `project_tracking.csv`, and backup notes are local tracking artifacts.
- `.venvs/autogluon` exists locally; old `.venvs/chronos2` and `.venvs/tide` were removed.
- Remaining warnings are upstream torch deprecations emitted during Chronos2 tests.
